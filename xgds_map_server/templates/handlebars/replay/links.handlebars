<div class="plan-link" id="links-group-flight">
  <div class="tool-entry">
      {{#each group_flight_named_urls}}
      	<a href="{{this}}" target="_new">{{@key}}</a><br/>
	{{/each}}
  </div>
  <div class="tool-entry">
      {{#each group_flight_links}}
      	<a href="{{this}}" target="_new">{{@key}}</a><br/>
	{{/each}}
  </div>
</div>
